import { FC } from 'react';

const PasswordIndicator: FC<PasswordIndicatorProps> = ({ strength }) => {
    return (
        <div>
            I indicate password strength.
        </div>
    )
}

export default PasswordIndicator;
